providers:
  local-minio:
    type: minio
    endpoint: host.docker.internal:9000
    public_endpoint: localhost:9000
    access_key: admin
    secret_key: admin123
    use_ssl: false
    bucket: my-bucket
    region: ""
    label: 本地 Docker MinIO
default_provider: local-minio
routing_rules: []
