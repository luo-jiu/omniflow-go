providers:
  local-minio:
    type: minio
    endpoint: localhost:9000
    access_key: admin
    secret_key: admin123
    use_ssl: false
    bucket: my-bucket
    label: "本地 MinIO"

default_provider: local-minio

routing_rules: []
